base=float(input("enter base value"))
height=float(input("enter base value"))
area (base*height)/2
if base>0 or height>0: 
    print("area of traingle is", area)
else: 
    print("values should be positive integers")
